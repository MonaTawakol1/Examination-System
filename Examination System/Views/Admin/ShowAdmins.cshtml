@model IEnumerable<User>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <title>ShowAdmins</title>
</head>
<body>
    <a asp-action="createAdmins" asp-controller="Admin" class="btn btn-danger">create</a>
    <table class="table table-bordered table-hover">
        <thead>
            <tr>

                <td>Admin Name</td>
                <td>Admin Email</td>
                <td>Operations</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>

                    <th>@item.Name</th>
                    <th>@item.Email</th>
                    <th>

                        <a asp-action="EditAdmin" asp-controller="Admin" asp-route-id="@item.Id" class="btn btn-info">Edit</a>
                        <a asp-action="DeleteAdmin" asp-controller="Admin" onclick="return confirm('are you sure')" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                    </th>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
